spring:
  batch:
    job:
      names: ${job.name:NONE}
  datasource:
    url: jdbc:mysql://localhost:3306/spring_batch
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
